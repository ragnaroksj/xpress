define([
  "jquery",
  "models/expedition-model"
  ],function($, ExpeditionItem){ 

    /*global require */
  "use strict";

  var expeditionCollection = Backbone.Collection.extend({
      
  });
 
  return expeditionCollection;

});
