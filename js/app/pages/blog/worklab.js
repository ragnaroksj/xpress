define(
	["jquery"],
	function($){
  //default js file for defualt page. 
		$(function(){

			/*var lookuptable = {},
			filterPanel = $(".filter-panel");
			$(".filter").each(function(){
				if( lookuptable[$(this).data("filter")] == undefined ) {
					lookuptable[$(this).data("filter")] = 1;
					filterPanel.append("<div class='filter-panel-item' data-filter='"+$(this).data("filter")+"'>"+$(this).data("filter")+"</div>");
				}
				else lookuptable[$(this).data("filter")]++;
			});*/


		});
		
});
